import * as React from "react"
import { Loader2, RefreshCw, UserPlus, Edit, Trash2, CheckCircle2, XCircle, Search } from "lucide-react"

export const Icons = {
  spinner: Loader2,
  refresh: RefreshCw,
  userPlus: UserPlus,
  edit: Edit,
  trash: Trash2,
  check: CheckCircle2,
  x: XCircle,
  search: Search,
}
